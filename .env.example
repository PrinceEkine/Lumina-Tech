# ---------------------------------------------------------------------------
# Kyventis Tech — environment variables
# Copy to .env.local for local dev, and add these in the Vercel dashboard
# (Project → Settings → Environment Variables) for production.
# ---------------------------------------------------------------------------

# --- Firebase Admin SDK (server / serverless functions) ---
# From a kyventis-tech service-account JSON (Firebase console →
# Project settings → Service accounts → Generate new private key).
FIREBASE_PROJECT_ID=kyventis-tech
FIREBASE_CLIENT_EMAIL=
# Keep the literal \n escapes; the app converts them to real newlines.
FIREBASE_PRIVATE_KEY=""
# Optional: leave blank to use the project's default Firestore database.
FIREBASE_DATABASE_ID=

# --- Email: Resend (primary provider) ---
# Get a key at https://resend.com/api-keys
RESEND_API_KEY=
# Verified sender. Until a custom domain is verified in Resend, keep the
# sandbox address below. NOTE: in sandbox mode Resend only delivers to the
# email you registered your Resend account with — so set CONTACT_TO_EMAIL
# (below) to that same address to receive contact submissions. The
# auto-reply to site visitors won't send until you verify a domain; the
# form still succeeds (that step is best-effort).
CONTACT_FROM_EMAIL="Kyventis Tech <onboarding@resend.dev>"
# Studio inbox that receives contact-form submissions. In sandbox mode this
# MUST equal your Resend account email. After verifying a domain, set
# CONTACT_FROM_EMAIL to e.g. "Kyventis Tech <hello@yourdomain>" and this can
# be any inbox.
CONTACT_TO_EMAIL=your-resend-account@email.com

# --- Email: Gmail SMTP (automatic fallback if RESEND_API_KEY is unset) ---
SMTP_USER=your-address@gmail.com
# A Gmail "App Password", NOT your normal password.
SMTP_PASS=

# --- Optional: Gemini API key (used by vite define at build time) ---
GEMINI_API_KEY=
